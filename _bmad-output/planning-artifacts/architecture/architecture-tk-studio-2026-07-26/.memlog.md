---
scope: tk-studio v1 target-state architecture: repo layout, config schema, planning-data adapter contract + canonical frontmatter interchange shape, store paths, ClaudeOS driver contract, rulings O2/O3/O4/O5/O8 under the O1 composite-distribution ruling and the four pillars
purpose: build-substrate
altitude: initiative
updated: 2026-07-26T18:46
---

- (direction) Headless create run. Inputs: brief.md, addendum.md A1-A8, o1-decision-2026-07-26.md (BINDING), planning-backend-research-2026-07-25.md, legacy-council-inventory-2026-07-25.md, legacy-council local extract (target-state-architecture.md three-tier memory, vendor_bmad.py, issues-ledger.md, seed-template.md). Altitude: initiative. Purpose: build-substrate.
- (constraint) O1 RULED (do not reopen): composite distribution - tk-studio layer as Claude Code plugin from git marketplace; BMad base via thin lockfile pin with upstream installer run at the pin; zero forks; measurement subsystem first-class (instrument, don't predict); activation health/drift check cross-checks both planes.
- (constraint) Inherited settled rulings: four named pillars (Lockstep/Taxonomy/Recommendation/Operations); kb at {project-root}/kb/ agent-first markdown + llms.txt-style index; Backlog.md default local task backend per-project binding; per-project VCS config (git default, Perforce first-class); layer stack BMad->tk-studio->connector->ClaudeOS with connector out of v1, driver contract in; two v1 roles (direction-giver, developer); dual-mode invariant; local-first build order.
- (decision) Paradigm: ports-and-adapters (hexagonal) over a plain-files data plane. Core = orchestration + canonical data contracts (frontmatter markdown, JSONL); every variable dependency behind a named binding: planning backend, VCS, scheduler substrate, harness driver, persona shell. Binds all; prevents backend/harness specifics leaking into skills.
- (decision) O2 RULING: file-plane sync layer. BMad-native planning artifacts (canonicalized frontmatter) are the canonical local representation skills read/write natively; stock skills untouched (zero-fork per S3); adapter is a standalone skill+library the orchestrator sequences (pre/post planning flows or on demand); bound external backend is a projection. Sync semantics: one-way promote local->backend plus status pull-back; no continuous bidirectional merge; conflicts surfaced never silently merged. Grounds: research finding that bidirectional file<->tracker sync barely exists; wrap/customize touch every skill (n-times surface), sync layer touches zero.
- (decision) O3 RULING: first external backend = Jira on Atlassian Cloud via the official Atlassian Remote MCP Server (GA 2026-02, Cloud-only, OAuth 2.1 or API tokens). Headless paths MUST use API-token auth - tk ISS-008 evidence: MCP OAuth cannot complete in non-interactive sessions. Confluence publishing of kb/ deferred (kovetskiy/mark path known); Linear second via same contract (epic->Project mapping).
- (decision) O4 RULING: cross-project shared knowledge OUT of v1 as a subsystem. Per-project kb/ only; cross-project read side served by the project registry in working data (canonical path for status-of-all-projects queries) plus the Obsidian vault as human window. Seam reserved: canonical kb frontmatter carries scope field; promotion-gate pattern (tk Reina membrane) named as the v2 mechanism; evolve loop observe-and-log only.
- (decision) O5 RULING: lean stateless orchestrator core wrapped in a separate optional persona shell (Tim's flagged third option). Shell is data (persona assets + customize), owns voice and the convene-the-council interaction, loads only in attended sessions; headless bypasses the shell entirely and MUST produce identical routing/outputs. No sanctum/rebirth machinery in v1 (tk-overlay material); role identity lives in the per-user store, not the shell.
- (decision) O8 RULING: hybrid substrate. tk-studio defines a canonical job model as data (job definition: id, skill target, trigger one-shot/cron/loop, cadence fixed or self-paced, budget guards, stop conditions, model/effort, status contract); execution delegates to the bound substrate. v1 default binding: harness-native primitives (scheduled tasks/cron, loop skills, self-paced wakeups). ClaudeOS mission runner becomes a second driver behind the driver contract once the connector exists. Job-definition homes: generic job types ship with the plugin (studio data); per-project job instances in project config; run state in per-user store run workspaces.
- (event) Web verification pass complete (subagent): all named-tech claims hold as of 2026-07-26. BMad latest v6.10.0 (no v7 date; v6.10 deprecates bmad-automator for new bmad-loop module); Backlog.md v1.48.0 active, still no epic entity, plain .md storage; Atlassian Remote MCP GA Cloud-only, OAuth2.1 or API tokens, scope now incl. Bitbucket/Compass/JSM; plugin marketplaces: extraKnownMarketplaces auto-prompt + strictKnownMarketplaces confirmed, plugin version field gates updates, sha pin on plugin sources; harness scheduling: CronCreate/loop/ScheduleWakeup exist but local scheduled tasks are SESSION-SCOPED, cloud routines are the persistent option; Obsidian junctions work without admin, officially cautioned (Sync/file-watch edge); Linear MCP live r/w.
- (version) Pinned at authoring: BMad Method 6.10.0 (installed in this repo, non-interactive flags verified); Backlog.md 1.48.0; Atlassian Remote MCP (hosted, GA 2026-02-04); Claude Code plugin marketplace schema per code.claude.com docs 2026-07.
- (decision) Store paths (Taxonomy homes, generalizing tk three-tier design): per-user store ~/.tk-studio/ (Windows %USERPROFILE%\.tk-studio\) holding config.yaml (user identity+role, vault path, machine id), registry/projects.yaml (project registry: known projects + bindings - canonical cross-project read path), projects/<project-key>/ (per-project working state: runs/, scratch/, session state), measurements/<user>-<machine>.jsonl (local ledger). Project: kb/ at {project-root}/kb/ with kb/index.md; planning per binding (default backlog/ Backlog.md-compatible); .tk-studio/config.yaml tracked project config + .tk-studio/config.local.yaml ignored per-dev overlay (tk config.local precedent; on Perforce projects P4IGNORE equivalent required). Studio data delivered via plugin. project-key defaults to project-root basename, project_id config override disambiguates (tk rule).
- (decision) Config schema three scopes: user (~/.tk-studio/config.yaml), project (.tk-studio/config.yaml tracked + config.local.yaml per-dev), studio (ships with plugin: bmad.lock pin, module registry, job types, resource defaults). Project config keys: project_id?, vcs (git|perforce, default git), planning binding {backend: bmad-files|backlog-md|jira|..., ids per backend}, kb_root override?, working_set (confirmed resource recommendations + roles), jobs[], model/effort overrides. General resolution: runtime override > project local > project tracked > user > studio default; model/effort precedence per brief: runtime > project config > resource default.
- (decision) Canonical planning interchange shape v1 (frontmatter schema, versioned shape_version: 1): entities epic|story|task, one file per entity, keys: id (EP-NNN/ST-NNN/TA-NNN stable, never reused), type, title, status enum {draft, ready, in-progress, blocked, review, done, dropped}, parent (story->epic, task->story), depends_on[], priority (P0-P3), assignee?, labels[], created/updated (ISO), external map per binding {jira: {key, synced_at, content_hash}}, body markdown with ## Acceptance Criteria checklist. Adapters MAP the canonical enum onto backend states, never extend it; unknown backend states map to nearest canonical + original preserved under external. Backlog.md mapping: epic->milestone+label (no native epic entity); Jira: epic->Epic, story->Story, task->Sub-task; Linear later: epic->Project.
- (decision) Vault linking resolution (reconciles brief's fixed {project-root}/kb/ with A2's one-link-per-project recommendation): <vault>/projects/<name>/ is a real vault folder containing per-category directory junctions: kb -> {project-root}/kb/ and backlog -> {project-root}/backlog/. One place per project in the vault, two junctions managed by the studio (created at onboarding, verified at activation). Vault is a view, never the database; registry is canonical for cross-project queries. Windows junctions (no admin) per A2; known caution: Obsidian Sync/file-watch edges.
- (decision) Measurement subsystem (per O1 ruling): event taxonomy v1 = install-outcome, drift-detection, activation-failure, headless-failure, onboarding-funnel (step timings, retries), report (human-authored via tk report verb). Local ledger append-only JSONL at ~/.tk-studio/measurements/<user>-<machine>.jsonl, atomic appends, sanitization at emission (ClaudeOS anonymization spine), never a shared file. Reconciliation: measurement-push skill (ledger -> feature branch -> commit -> PR into tk-studio repo measurements/); consolidation skill (accumulated PRs -> issues ledger entries -> fix candidates); PR review is the promotion membrane. Issues ledger harvests tk format: issues/ledger.md, stable ISS-NNN ids, severity High/Medium/Low, status Open/Mitigated/Resolved/Wontfix, rows never deleted.
- (decision) Lockstep mechanics: bmad.lock (committed, tk-studio repo, shipped in plugin) pins BMad version + module registry + install flags; tk install runs npx bmad-method@<pin> install non-interactively with the project's module set (flags verified working this session); base-update skill = bump pin -> run upstream installer -> integration PR for review (recovers M1's reviewable-diff virtue without a fork). Activation front-door check is mechanism-independent and three-way: installed _bmad manifest vs pin; installed plugin version vs marketplace.json; report-only + guided fix, NEVER silent, NEVER auto-mutates. Marketplace plugin version field is the update gate; sha pin available on plugin sources if needed.
- (decision) Migration safety rules ported verbatim from tk (A3 PORT list): closed inventory before any move; no-delete-before-clearance (migrated AND verified AND cleared); copy-then-verify-then-flag cutover with flag written last (crash-safe); backend migration = export (canonical shape) -> transform -> import -> verification pass (counts, id map, content hash, round-trip spot check); source backend retained read-only until user clears.
- (decision) Naming + conventions: all studio skills/agents prefixed tk-studio-*; skill verbs kebab-case (tk-studio-activate, tk-studio-plan-sync, tk-studio-detect, tk-studio-onboard, tk-studio-migrate, tk-studio-report, tk-studio-measure-push, tk-studio-consolidate, tk-studio-base-update, tk-studio-job); JSON status block on every skill per BMad headless convention (status complete|partial|blocked, intent, artifacts[], reason?); measurement events one-JSON-object-per-line {ts, event, user, machine, project?, payload}; dates ISO-8601; ids stable never reused.
- (assumption) ASSUMPTION: skill roster names above are seed, not invariant - final naming at module-builder. ASSUMPTION: Backlog.md tolerates superset frontmatter keys (unknown keys preserved) - verify before binding backlog-md adapter implementation; if false, adapter keeps canonical sidecar mapping instead. ASSUMPTION: plugin can ship bmad.lock readable machine-wide via {skill-root} resolution.
- (question) OPEN (architecture defers, module-builder decides): exact bmad.lock file format (YAML vs TOML) - trivial. OPEN: whether tk install wraps as a skill only or also a tiny CLI shim (skill-first per simplicity bias, CLI only if headless bootstrap needs it - a machine with no plugin yet cannot run a plugin skill; onboarding funnel may need one bootstrap script in the repo).
- (event) Reviewer gate dispatched: lint_spine.py clean (3 low false-positives - intentional {project-root} tokens); 4 parallel reviewer subagents launched (rubric-walker, verify-currency, adversarial, reconcile-inputs) writing to reviews/.
- (event) Reviewer gate complete. Verdicts: rubric-walker approve-with-revisions (3 high), verify-currency pass (4 minor), adversarial 1 critical + 4 holes, reconcile-inputs strong-with-gaps (1 contradiction, 1 dropped direction). Full reviews in reviews/. ALL findings applied: AD-4 now owns canonicalization + sole id authority (closes critical seam - normalize pass edits artifacts not BMad code, zero-fork holds); AD-5 extended to local projections with echo suppression, both-changed=human conflict, round-trip-stable mappings; AD-1 reworded (working answer revisable via measurement loop; base-update integration PR added); AD-3 credentials as explicit fifth class; AD-7 Rovo rename + token-toggle preflight + mapping tagged default; AD-10 v1 job families (research+maintenance); AD-12 one-emitter-per-event + payload schemas + observation event + taxonomy extension via PR; AD-17 role-keyed working_set map + user-authored resources first-class; NEW AD-19 conformance suite as shipped artifact; NEW AD-20 registry single-schema single-writer; conventions: id minting/dup-block, platform envelope row; Deferred: knowledge-lifecycle port seam, Backlog.md projection-fidelity check.
- (decision) Triage of non-applied review suggestions: none rejected outright; all critical/high/medium landed in the spine. Renderings step: headless run, no additional human-facing artifact requested - spine + memlog are the deliverables. External handoffs: none configured.
- (event) spine finalized
- (direction) Tim (2026-07-26, post-finalize): no bootstrap script in the repo - clone + folder trust auto-prompts the marketplace/plugin install, base install is plain npx at the pin; tk install stays a plugin skill only. Open question closed; AD-1 and Deferred amended (update intent, AD ids stable).
- (constraint) Full official module set installed in this repo (Tim's correction: match/exceed legacy-council capabilities): bmm + bmb 2.1.0, cis 0.2.1, gds 0.6.0, tea 1.19.1, wds 0.4.3, bmad-loop 0.9.0 (automator skipped - deprecated in favor of bmad-loop; bmad-loop-setup not yet run). LEARNING folded into AD-1: external modules version and channel independently of core, so bmad.lock pins per-module versions, not just the core version.
